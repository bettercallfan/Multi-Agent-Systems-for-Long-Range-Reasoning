# Optional document/video assembly tools; not needed to run the agent system.
# System binaries also required: pandoc, latexmk/xelatex, poppler, ffmpeg, node.
python-docx==1.2.0
python-pptx==1.0.2
playwright==1.60.0
Pillow>=10,<13
