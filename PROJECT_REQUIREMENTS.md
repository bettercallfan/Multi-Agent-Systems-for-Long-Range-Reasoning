# 动态异构群体智能系统——项目需求基线

> 赛题编号：XH-202631  
> 赛题名称：面向超长程复杂任务的动态异构群体智能架构与深度协同推理技术  
> 原始需求来源：`XH-202631荣耀终端股份有限公司-面向超长程复杂任务的动态异构群体智能架构与深度协同推理技术比赛方案(2).pdf`  
> 文档版本：v1.0  
> 基线日期：2026-07-14  
> 文档状态：后续开发的需求单一基线（Single Source of Truth）

## 1. 文档用途与维护规则

本文件将比赛方案转化为可开发、可测试、可验收的工程需求。后续架构设计、任务拆分、代码实现、测试、演示和答辩材料均应引用本文件中的需求编号。

维护规则：

1. 比赛 PDF 是原始事实来源，本文件是项目工程基线；二者冲突时以比赛 PDF 为准。
2. 标记为“赛题硬性要求”的内容不得因实现困难而删除。
3. 标记为“工程化推导”的内容是为了满足赛题而制定的实现要求，可在保留目标的前提下调整方案。
4. 每次改变系统范围、架构或验收标准时，应同步更新本文件的版本与变更记录。
5. 任何新功能应至少关联一个 `FR`、`NFR` 或 `DEMO` 需求；无法关联的功能原则上不进入当前版本。
6. “Agent 在对话中声称完成”不构成验收证据；状态、执行结果和产物必须由框架记录并验证。

本文使用以下等级：

- **MUST**：赛题硬性要求或完成系统闭环不可缺少的能力。
- **SHOULD**：对评分有显著影响，应在正式提交前完成。
- **COULD**：增强项，在核心闭环稳定后实现。

## 2. 赛题目标解读

### 2.1 最终目标

构建一个高鲁棒性的群体智能自治引擎，将高维、模糊的自然语言意图转化为复杂、非确定性环境中的可验证交付物，并在无人工干预的情况下完成长程任务闭环。

系统不能只是一组固定 Agent 的顺序调用，也不能依赖无约束的自由群聊。目标形态应是：

```text
用户高层意图
    ↓
任务理解与契约化
    ↓
动态任务图生成
    ↓
异构 Agent / 工具 / 模型 / 资源动态路由
    ↓
执行、反馈、纠错、重规划、恢复
    ↓
可验证交付物 + 完整决策轨迹
```

### 2.2 赛题明确反对的方案

- 只依赖单个大模型的超长上下文窗口维持全部状态。
- 固定 Agent 队伍加静态全连接通信。
- 所有 Agent 共享全部消息的高 Token 群聊。
- 只有规划，没有真实执行和反馈闭环。
- 流程失败后依赖人工介入才能继续。
- 只演示一个垂直、短流程或低复杂度任务。
- 直接套用开源框架而缺少架构或核心算法创新。

### 2.3 项目成功的工程定义

系统成功不等同于“生成了一份最终报告”。一次任务至少同时满足：

1. 高层意图被转化为明确任务契约和成功标准。
2. 规划结果形成可执行、可更新的任务图。
3. 系统根据节点能力需求动态选择执行者与资源位置。
4. 中间状态、关键决策和产物可持久化、可恢复。
5. 环境异常、需求变更或节点失效可触发自动诊断与重规划。
6. 最终交付物通过确定性验收。
7. 全过程可追踪，并能统计成功率、时间、Token 和资源消耗。

## 3. 系统范围

### 3.1 范围内

- 自然语言复杂任务接收与结构化。
- 多文件、多模态输入的解析与标准化。
- 动态任务分解、依赖建模和增量重规划。
- 异构 Agent、模型、工具与端边云节点注册和调度。
- 稀疏、结构化、面向任务的通信。
- 分层记忆、上下文压缩、检索和唤醒。
- 真实代码或工具执行、环境反馈和产物管理。
- 错误归因、重试、降级、替代节点和断点恢复。
- 技术审查、语义审查和最终交付验收。
- 运行轨迹、指标统计和可视化展示。
- 至少两个跨领域长程复杂任务演示。

### 3.2 当前不作为第一阶段核心目标

- 自研基础大模型训练。
- 自研通用操作系统或完整云平台。
- 一开始就实现真实移动终端、边缘集群和云集群的生产级部署。
- 无边界地增加 Agent 数量。
- 在可靠状态机完成前实现完全自治的自由演化群体。

上述项目可通过模拟资源节点、统一适配接口和可替换执行后端逐步扩展。

## 4. 总体架构要求

系统采用“固定安全外壳 + 动态任务执行图”的双层架构。

### 4.1 固定安全外壳

以下生命周期由 Python 框架控制，MUST 保持显式：

```text
prepare → classify → plan → execute_graph → review → report → final_validate → finish
```

固定外壳负责状态合法性、权限边界、持久化、审查和最终验收。Agent 不得自行推进外层阶段。

### 4.2 动态任务执行图

`plan` 阶段必须生成可执行任务图；`execute_graph` 根据依赖关系动态调度，而不是固定调用同一组 Agent。

任务图节点至少包含：

```json
{
  "node_id": "extract_documents",
  "description": "提取并标准化输入资料",
  "capability": "document_extraction",
  "dependencies": [],
  "inputs": [],
  "expected_outputs": [],
  "success_criteria": [],
  "resource_requirements": {},
  "security_level": "internal",
  "retry_policy": {},
  "status": "pending"
}
```

任务图必须支持：

- DAG 依赖关系。
- 条件分支。
- 可并行节点。
- 节点失败重试。
- 局部重规划。
- 新需求动态插入。
- 节点替换与降级。
- 从持久化检查点恢复。

## 5. 功能需求

### 5.1 意图理解与任务契约

#### FR-INTENT-001（MUST）原始输入统一化

系统必须接收文件、文件夹和自然语言任务，并生成统一的 `TaskSpec`。

验收：不同输入形式均可形成包含任务目标、输入清单、任务类型、交付物和成功标准的 TaskSpec。

#### FR-INTENT-002（MUST）任务契约单一来源

任务类型、代码策略、必需产物、报告章节、成功标准和插件选择必须只有一个权威来源；后续组件不得重复猜测。

#### FR-INTENT-003（SHOULD）模糊需求澄清

系统应识别缺失、冲突或不可验证的需求，并选择自动补全合理默认值、提出澄清或标记风险。

#### FR-INTENT-004（MUST）成功标准可机器验证

TaskSpec 中的关键成功标准必须尽可能转换为文件存在性、Schema、数值不变量、测试结果或环境状态等机器可验证条件。

### 5.2 动态规划与任务图

#### FR-PLAN-001（MUST）结构化任务图

PlanningAgent 必须输出结构化任务图，而非仅输出自然语言步骤。

#### FR-PLAN-002（MUST）动态依赖调度

框架必须根据节点依赖和状态选择可执行节点；规划 Agent 不得直接调用其他 Agent。

#### FR-PLAN-003（MUST）增量重规划

当执行失败、输入变化、需求变更或节点失效时，系统必须能够保留已验证成果，只对受影响子图重新规划。

#### FR-PLAN-004（SHOULD）并行执行

无依赖且资源允许的节点应支持并行调度，并记录并发带来的时间与资源收益。

#### FR-PLAN-005（SHOULD）规划质量检查

执行前应检查任务图是否存在循环依赖、缺少输入、无执行者、不可验证产物或资源不满足等问题。

### 5.3 动态异构智能体组织

#### FR-AGENT-001（MUST）能力注册

每个 Agent、工具和执行器必须注册能力、输入输出类型、模型需求、资源要求、成本和安全属性。

#### FR-AGENT-002（MUST）动态组队

框架必须按任务节点需要选择最小可用执行集合，不得为所有任务固定启用全部 Agent。

#### FR-AGENT-003（MUST）角色权限边界

Agent 只能返回其职责范围内的结构化结果。状态推进、文件落盘、真实执行和最终成功判定必须由框架完成。

#### FR-AGENT-004（SHOULD）可插拔角色

新增或移除 Agent 不应修改主工作流；应通过注册表或插件发现机制接入。

#### FR-AGENT-005（SHOULD）执行者替换

一个节点的首选 Agent 失效时，调度器应根据能力匹配选择备选 Agent、工具或规则执行器。

### 5.4 稀疏路由与低熵通信

#### FR-COMM-001（MUST）非全连接通信

消息只发送给任务图中存在依赖或明确订阅关系的接收者，禁止默认广播完整历史。

#### FR-COMM-002（MUST）结构化消息协议

Agent 间消息至少包含：发送者、接收者、关联节点、消息类型、摘要、证据引用、置信度和可见范围。

#### FR-COMM-003（MUST）证据引用而非内容复制

大文件、中间数据和历史轨迹应通过 Artifact ID、Memory ID 或路径引用；消息中只传递必要摘要。

#### FR-COMM-004（SHOULD）信息压缩与去重

系统应检测重复、低价值和过期信息，压缩后再进入后续上下文。

#### FR-COMM-005（SHOULD）通信效率指标

系统应统计每个节点及整轮任务的消息数量、Token、重复率、有效证据引用率和压缩比。

### 5.5 长程记忆与上下文连续性

#### FR-MEM-001（MUST）分层记忆

记忆至少分为：

- 工作记忆：当前节点所需的短期上下文。
- 情节记忆：执行事件、错误、决策与恢复记录。
- 语义记忆：稳定事实、规则和任务知识。
- 产物记忆：文件、数据、代码、测试和报告的可追踪索引。

#### FR-MEM-002（MUST）关键状态持久化

全局目标、TaskSpec、TaskGraph、RunState、节点结果、验证结论和产物元数据必须持久化，不依赖模型对话历史保存。

#### FR-MEM-003（MUST）上下文按需唤醒

调度节点时只检索与节点目标、输入和依赖相关的记忆，不加载整轮历史。

#### FR-MEM-004（SHOULD）压缩无损性检查

上下文压缩后应保留目标、约束、关键决策、未解决问题和证据指针，并能检测关键信息丢失。

#### FR-MEM-005（SHOULD）长程连续性测试

系统应提供数百至数千节点/事件的合成或真实压力测试，验证目标、约束和关键状态不漂移。

### 5.6 执行、反馈与产物

#### FR-EXEC-001（MUST）真实执行

代码和工具调用必须真实执行并记录命令、退出码、stdout、stderr、时间、资源和生成文件；自然语言声明不算执行成功。

#### FR-EXEC-002（MUST）产物契约

每个任务节点必须声明输入、预期产物、Schema 和成功标准；产物由框架登记哈希、来源节点和版本。

#### FR-EXEC-003（MUST）执行隔离

生成代码只能访问允许的输入、输出和资源，禁止修改框架状态、系统文件或其他运行目录。

#### FR-EXEC-004（MUST）确定性验收

只要存在可确定验证的规则，就应使用 Python、Schema、测试或规则引擎验证，而不是交给 LLM 主观判断。

#### FR-EXEC-005（SHOULD）多执行后端

执行层应逐步支持 Python、Shell、HTTP 工具、数据库、远程节点和模拟终端；任务图节点不应绑定单一 Python 脚本。

### 5.7 容错、纠错与恢复

#### FR-FAULT-001（MUST）异常分类

系统必须区分模型连接、规划、代码生成、执行、数据、产物质量、资源、权限、节点失效和报告等错误类型。

#### FR-FAULT-002（MUST）局部恢复路由

失败记录必须包含 `failure_type`、`repair_target`、`resume_node/stage`、影响范围和剩余预算。

#### FR-FAULT-003（MUST）自动重试与降级

重试次数由策略统一控制；达到上限后应尝试替代执行者、降级方案或局部重规划，而非无限重试。

#### FR-FAULT-004（MUST）节点失效注入

系统必须支持运行中注入 Agent/工具/模型节点失效，并在无人干预下完成替换或恢复。

#### FR-FAULT-005（MUST）需求变更注入

系统必须支持执行中修改目标、约束或交付物，计算受影响子图并增量执行。

#### FR-FAULT-006（MUST）断点续跑

进程或服务器中断后，系统应从最近一致检查点恢复，不重复已验证且未受影响的节点。

### 5.8 端—边—云异构调度

#### FR-RESOURCE-001（MUST）资源节点抽象

系统应统一描述端、边、云资源，包括模型、算力、延迟、成本、可用性和数据安全等级。

#### FR-RESOURCE-002（MUST）位置选择策略

框架应依据实时性、复杂度、数据敏感度、资源状态和预算选择推理或执行位置。

#### FR-RESOURCE-003（SHOULD）动态资源变化

节点离线、负载升高或预算变化时，调度器应重新选择可行位置。

#### FR-RESOURCE-004（SHOULD）模型路由

简单节点优先使用低成本模型或规则工具，复杂推理节点使用能力更强的模型，并记录选择理由。

#### FR-RESOURCE-005（COULD）模型切分

在具备真实环境条件时，探索模型切分或端云协同推理；第一阶段可用可解释的模拟调度证明架构能力。

### 5.9 审查、报告与最终验收

#### FR-REVIEW-001（MUST）执行后审查

审查必须基于真实执行结果、产物和证据，且发生在最终报告生成之前。

#### FR-REVIEW-002（MUST）审查权限隔离

Reviewer 只提供结构化语义建议；确定性门禁由框架和任务插件执行。

#### FR-REVIEW-003（MUST）最终报告唯一生成

最终报告只在预审允许时生成一次，且不得写入 TaskSpec、RunState、Trace 等系统内容。

#### FR-REVIEW-004（MUST）最终验收

最终验收至少检查必需产物、Schema、执行结果、报告章节、引用文件、禁止内容和失败状态。

### 5.10 可观测性与用户界面

#### FR-OBS-001（MUST）完整推理与决策轨迹

系统必须展示阶段、任务图、节点状态、路由选择、模型/工具调用、错误、恢复和产物关系。

#### FR-OBS-002（MUST）轨迹分层

用户视图、调试视图和敏感内部信息应分离，展示决策摘要和证据，不泄露密钥或不必要的模型内部内容。

#### FR-OBS-003（SHOULD）可视化界面

应提供逻辑清晰的界面展示任务图、执行进度、Agent 拓扑、资源位置、成本与最终交付物。

#### FR-OBS-004（SHOULD）人工接管接口

虽然正式演示应无人干预，系统仍应提供暂停、审批、恢复和人工修正接口，用于高风险生产场景。

## 6. 非功能需求

### NFR-REL-001 长程稳定性（MUST）

在目标演示任务中，无明显过程遗忘、目标漂移、规划幻觉或状态链断裂；所有关键状态可由持久化数据恢复。

### NFR-REL-002 鲁棒性（MUST）

异常注入后系统能够自动恢复或给出明确、可恢复的失败结论，不得误报成功。

### NFR-EFF-001 Token 效率（MUST）

记录每节点和整轮 Token 使用量；通过稀疏路由、摘要、引用和缓存减少重复上下文。

### NFR-EFF-002 时间效率（SHOULD）

记录节点排队、模型调用、工具执行和总耗时；可并行节点应能获得可测量的加速。

### NFR-COMP-001 模型兼容性（MUST）

通过统一 ModelClient 适配至少两种主流或 OpenAI 兼容模型服务，业务逻辑不得依赖某个模型名称。

### NFR-EXT-001 可扩展性（MUST）

新增任务插件、Agent、工具、模型或资源节点时，不修改主状态机和核心调度循环。

### NFR-SEC-001 密钥安全（MUST）

API Key 只允许通过环境变量或密钥管理服务注入，不得提交到仓库、日志、Trace、报告或任务产物。

### NFR-SEC-002 数据与执行安全（MUST）

执行器应限制路径、命令、网络、资源和超时；敏感任务应优先路由到端侧或合规节点。

### NFR-REP-001 可复现性（MUST）

每轮运行保存冻结的 TaskSpec、任务图版本、模型和配置标识、代码、执行记录、产物哈希和验收结论。

### NFR-UX-001 易用性（SHOULD）

用户只需提供高层任务和输入资料即可启动；界面应清楚显示当前状态、风险、失败原因和交付物。

## 7. 演示场景需求

赛题评分明确鼓励两个及以上高完成度跨领域长程任务。正式方案必须至少准备两个跨领域场景，建议三个。

### DEMO-001 跨模态文档与业务决策（MUST）

示例：差旅报销、合同审查、投研资料分析等。

必须体现：

- PDF、Word、Excel 等多源输入。
- 信息抽取、规则核对、结构化产物和报告。
- 输入缺失、格式异常或规则冲突注入。
- 产物 Schema 和业务不变量验收。

当前 `expense_reimbursement` 可作为该类场景的早期样例，但需要扩展为更长任务链。

### DEMO-002 系统级软件工程（MUST）

示例：从自然语言需求生成、修改、测试并交付一个多模块软件项目。

必须体现：

- 需求分析、架构、编码、测试、审查、修复和交付。
- 多文件代码库与真实测试执行。
- 中途需求变更。
- 编码或测试节点失效后的替换与恢复。

### DEMO-003 数学建模或数据科研（SHOULD）

示例：MathorCup 题目、预测建模、优化调度或跨文献数据分析。

必须体现：

- 问题理解、数据处理、模型构建、实验与结果解释。
- Reasoning、Code、Review 等异构能力动态协作。
- 多方案比较、失败实验保留与局部重规划。

### 通用异常注入矩阵

每个正式演示至少覆盖两类异常：

| 异常类型 | 注入方式 | 期望系统行为 |
|---|---|---|
| 模型连接失败 | 模拟超时/5xx | 短重试后切换模型或明确失败 |
| Agent 节点失效 | 禁用执行者 | 选择备选执行者并保留上下文 |
| 工具执行失败 | 返回非零退出码 | 归因、修复、重试或降级 |
| 产物质量失败 | 注入非法或不一致数据 | 由确定性校验拒绝并路由修复 |
| 需求变更 | 中途新增约束或交付物 | 更新 TaskSpec，重规划受影响子图 |
| 服务器中断 | 终止并重启进程 | 从检查点续跑 |

## 8. 评分标准与需求追踪

### 8.1 作品完整性：40 分

| 评分项 | 分值 | 本项目对应能力 |
|---|---:|---|
| 系统基础与闭环能力 | 15 | FR-INTENT、FR-PLAN、FR-EXEC、FR-FAULT、NFR-REL |
| 组织架构与协作机制 | 15 | FR-AGENT、FR-COMM、动态任务图 |
| 多场景任务演示 | 10 | DEMO-001、DEMO-002、DEMO-003 |

目标：至少完成两个高完成度跨领域长程任务，并展示泛化或组合能力。

### 8.2 应用创新性：25 分

| 评分项 | 分值 | 本项目对应能力 |
|---|---:|---|
| 场景覆盖与业务契合度 | 10 | 跨领域插件体系、通用任务契约 |
| 社会价值与经济效益 | 10 | 数字劳动力平台、成本和效率指标 |
| 用户体验与易用性 | 5 | FR-OBS、NFR-UX |

### 8.3 技术创新性：20 分

| 评分项 | 分值 | 本项目对应能力 |
|---|---:|---|
| 架构与交互降噪创新 | 10 | 动态稀疏路由、低熵消息协议、证据引用 |
| 核心算法与底层突破 | 10 | 分层记忆、上下文唤醒、动态调度、增量重规划 |

### 8.4 系统性能与效率：15 分

| 评分项 | 分值 | 本项目对应能力 |
|---|---:|---|
| 执行成功率与鲁棒性 | 5 | 异常注入、恢复率、任务成功率 |
| Token 与时间效率 | 5 | FR-COMM-005、NFR-EFF |
| 兼容性与可扩展性 | 5 | NFR-COMP、NFR-EXT |

## 9. 核心量化指标

正式提交前至少采集以下指标：

| 指标 | 定义 | 初始目标 |
|---|---|---:|
| 任务成功率 | 通过最终验收的运行数 / 总运行数 | ≥ 80% |
| 异常恢复率 | 自动恢复成功异常数 / 可恢复异常总数 | ≥ 80% |
| 误报成功率 | 实际不合格但标记成功的比例 | 0% |
| 检查点恢复成功率 | 中断后成功续跑比例 | ≥ 90% |
| 关键状态保持率 | 长程测试中未丢失关键约束比例 | 100% |
| 重复消息率 | 重复或近重复消息 / 全部消息 | 持续下降并可对比 |
| 上下文压缩比 | 原始上下文 Token / 实际注入 Token | ≥ 3:1 |
| 跨领域高完成度场景 | 通过场景验收的不同领域任务数 | ≥ 2 |
| 模型兼容数量 | 可完成同一基准任务的模型服务数 | ≥ 2 |
| Agent 插拔影响 | 新增 Agent 所需核心流程改动 | 0 处 |

以上阈值属于工程初始目标，不是赛题原文给出的官方阈值；应根据基准测试持续调整并保留历史结果。

## 10. 交付物要求

### 10.1 材料文档（赛题硬性要求）

- 群体智能总体架构设计。
- 动态拓扑路由算法。
- 长程记忆保持与上下文压缩唤醒策略。
- 低熵结构化通信方案。
- 端—边—云协同与资源调度方案。
- 核心算法伪代码。
- 核心算法复杂性分析。
- 跨领域场景设计与实验结果。
- 异常注入、鲁棒性和效率评测报告。
- 部署、使用和复现实验说明。

### 10.2 可运行系统（赛题硬性要求）

- 可部署的运行环境。
- 从高层意图到最终交付物的无人干预闭环。
- 动态异常、需求变更和节点失效注入能力。
- 自动纠错、重规划和恢复能力。
- 中间决策过程与推理轨迹展示。
- 至少两个不同类型长程复杂任务的完整演示。

### 10.3 项目工程交付物

- 源代码与依赖锁定文件。
- 配置模板，不包含真实密钥。
- 自动化测试与基准测试。
- 示例任务、输入和预期结果。
- 架构图、任务图和通信拓扑图。
- 演示脚本及录屏方案。
- 每次正式演示的 RunState、Trace、Artifact、Validation Report 和指标文件。

## 11. 当前框架与目标差距

截至本基线版本，当前项目已经具备：

- 固定外层显式状态机。
- TaskSpec、RunState 和产物契约。
- 结构化规划、错误归因和审查结果。
- 代码真实保存、执行和退出码判定。
- 任务插件与确定性业务产物校验。
- 失败重试、根因路由和最终验收。
- 一次报销文档任务的成功闭环。

当前尚未满足或仅部分满足：

| 差距 | 当前状态 | 对应需求 | 优先级 |
|---|---|---|---|
| Planning 计划不能真正驱动动态图 | 计划主要是建议 | FR-PLAN-001~005 | P0 |
| Execute 固定为单脚本流程 | 只支持一个 Python Pipeline | FR-EXEC-005 | P0 |
| Agent 队伍与插件注册静态 | 存在硬编码注册 | FR-AGENT-002~005 | P0 |
| 缺少分层长期记忆与按需唤醒 | 仅有运行摘要和状态 | FR-MEM-001~005 | P0 |
| 缺少稀疏消息总线和通信指标 | 当前以阶段 Prompt 传递为主 | FR-COMM-001~005 | P0 |
| 缺少需求变更、节点失效和断点续跑 | 仅有阶段内重试 | FR-FAULT-004~006 | P0 |
| 缺少端边云资源抽象与调度 | 尚未实现 | FR-RESOURCE-001~005 | P1 |
| 缺少跨领域长程任务 | 当前主要验证报销场景 | DEMO-002、DEMO-003 | P0 |
| 缺少数千步压力和效率基准 | 尚未实现 | NFR-REL、NFR-EFF | P1 |
| 缺少面向展示的可视化界面 | 目前为文件和日志 | FR-OBS-003 | P1 |

结论：当前代码是一个可靠的“显式流水线基础框架”，但还不是赛题要求的“动态异构群体智能自治引擎”。下一阶段的核心不是继续增加业务 Agent，而是实现动态任务图、能力注册与路由、分层记忆、低熵通信和局部恢复。

## 12. 分阶段开发路线

### Phase 0：可靠基础框架（已基本完成）

- 显式外层状态机。
- TaskSpec、RunState、ArtifactContract。
- 真实执行、结构化结果、审查和最终验收。
- 任务插件与失败路由。

完成判据：现有自动化测试通过，报销样例可稳定生成可验证交付物。

### Phase 1：动态任务图与能力注册（P0）

- 定义 `TaskGraph`、`TaskNode`、节点状态和依赖。
- PlanningAgent 输出可验证任务图。
- 建立 Agent/Tool/Model Capability Registry。
- 实现就绪节点调度、并行执行和节点级产物契约。
- 把现有固定 execute 迁移为节点执行器。

完成判据：两个不同任务产生不同任务图，且无需修改主调度循环。

### Phase 2：记忆与低熵通信（P0）

- 工作、情节、语义、产物四层记忆。
- 结构化消息总线与证据引用。
- 节点级上下文检索、摘要和压缩。
- Token、重复率和压缩比指标。

完成判据：长任务中不广播完整历史，关键状态保持率达到目标，并可证明 Token 降低。

### Phase 3：容错、重规划和断点恢复（P0）

- 需求变更影响分析。
- 节点失效替代执行。
- 子图级重规划。
- 一致性检查点和进程中断恢复。

完成判据：异常矩阵中的关键场景可无人干预恢复，已验证节点不会无意义重复执行。

### Phase 4：端边云资源调度（P1）

- Resource Registry。
- 延迟、成本、隐私、能力约束下的路由策略。
- 至少端/边/云模拟节点，条件允许时接入真实环境。
- 多模型自适应选择和降级。

完成判据：同一任务的不同节点可被解释性地路由到不同资源位置，资源变化时可重新调度。

### Phase 5：跨领域演示与可视化（P0/P1）

- 完善文档业务任务。
- 实现软件工程长程任务。
- 实现数学建模或数据科研任务。
- 建设任务图、拓扑、资源、成本和轨迹界面。
- 形成比赛材料、实验和演示脚本。

完成判据：至少两个跨领域任务高完成度通过，覆盖动态需求、节点失效或环境异常。

## 13. 首批架构决策约束

后续设计必须遵守：

1. 外层安全生命周期固定，任务执行图动态。
2. LLM 提供建议和内容，Python 框架拥有状态与控制权。
3. 确定性规则优先于 LLM 判断。
4. 所有任务状态必须可序列化、可恢复。
5. 节点之间传递结构化消息和证据引用，不传递无界聊天历史。
6. 核心调度器不感知具体报销、软件工程或建模字段。
7. 业务能力通过插件、Schema、规则和能力注册接入。
8. 每项动态能力必须有可观测指标和异常测试。
9. 未经最终验收的运行不得标记成功。
10. 密钥、敏感输入和内部推理信息不得进入公开交付物。

## 14. 待确认问题

以下内容赛题原文未给出明确技术口径，需要在研发中形成假设并在材料中说明：

1. “数千步复杂任务”的步定义：任务图节点、模型调用、工具调用还是状态事件。
2. 端、边、云是否必须使用真实硬件，还是允许可验证的模拟资源环境。
3. 是否提供官方测试集、隐藏任务或固定性能基准。
4. 推理轨迹展示需要到什么粒度，如何兼顾安全和模型私有推理。
5. 模型切分是硬性演示项还是可用资源调度和多模型路由替代。
6. 最终提交的部署环境、容器规范、演示时长和网络条件。
7. 允许使用的闭源模型、外部 API 和第三方工具范围。

在官方进一步说明前，项目应以可插拔接口和可复现实验覆盖这些不确定性。

## 15. 参赛与提交约束

以下属于比赛管理要求，不是系统功能需求，但项目排期和最终交付必须遵守：

- 参赛对象为符合方案要求的国内全日制在校学生；团队不超过 10 人。
- 每件作品可由不超过 3 名指导教师指导，可跨专业、学校和地域组队。
- 每件作品只能由 1 所高校作为参赛主体提交。
- 官网报名开放时间为 2026 年 5 月 30 日至 6 月 30 日；项目组应自行确认报名已完成且审核通过。
- 作品提交截止时间为 **2026 年 9 月 15 日前**。
- 提交内容包括审核通过并盖章的报名表 PDF 及作品全部相关材料。
- 作品材料发送至方案指定的发榜单位邮箱；邮件主题与压缩包按“学校-姓名-作品名-联系电话”命名。
- 初审预计在 2026 年 9 月 30 日前完成；终审擂台赛预计在 2026 年 11 月举行。
- 初审与终审采用相同评分标准，最终成绩占比分别为 40% 和 60%。

建议内部冻结节点：

| 内部节点 | 建议最晚时间 | 目标 |
|---|---|---|
| 核心架构冻结 | 2026 年 8 月上旬 | 动态任务图、记忆、通信和容错主链路稳定 |
| 跨领域演示冻结 | 2026 年 8 月中旬 | 至少两个场景端到端通过 |
| 性能实验冻结 | 2026 年 8 月下旬 | 形成成功率、鲁棒性、Token 和时间数据 |
| 文档与演示冻结 | 2026 年 9 月上旬 | 完成材料、部署包、演示脚本和复现检查 |
| 正式提交 | 2026 年 9 月 15 日前 | 按官方规范完成全部提交 |

内部日期属于本项目的工程建议，可根据团队实际进度调整，但不得影响官方截止时间。

## 16. 版本变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|---|---|---|---|
| v1.0 | 2026-07-14 | 根据 12 页比赛方案建立首版需求、验收、评分映射和路线图 | 项目组 |
