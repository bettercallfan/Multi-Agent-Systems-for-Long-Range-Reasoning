如果严格按照评分标准，优先级应该调整
第一优先级：真正实现数百至数千步长程任务
这是作品完整性 15 分和技术创新性 10 分共同关注的核心。
当前虽然已经有：
层级 PlanIR；
动态 DAG；
RuntimeMemory；
Context Capsule；
上下文压缩；
自动修复。
但真实任务通常仍然只有几到十几个业务节点，不能证明“数千步复杂任务”。
下一步应该重点实现：
长任务持续展开
→ 分阶段生成子图
→ 已完成阶段写入检查点
→ 后续阶段按需加载记忆
→ 失败时只重开局部子图
→ 任务可以跨进程持续运行
这里最重要的是：
断点续跑；
长程状态持久化；
阶段性子图扩展；
关键目标和约束持续唤醒；
防止规划一次性压缩为几个粗节点。
第二优先级：把动态拓扑做成“运行时动态”
任务书要求通信拓扑：
应随任务语义动态生成稀疏路由。

当前已经能够在规划阶段动态生成 DAG，但运行后拓扑变化能力仍然有限。
应补充：
质量下降
→ 增加验证节点或替换执行者

节点失败
→ 删除不可用路由并选择备选执行者

资源变化
→ 重新选择推理位置

需求变化
→ 插入新节点并重新计算受影响子图
这样才能从“动态生成一次 DAG”升级为“运行期间动态异构拓扑”。
第三优先级：端—边—云资源调度
这是答题要求 c 中明确写出的能力，而当前项目基本没有真正实现。
第一版不需要接真实手机和云集群，可以实现轻量 Resource Registry：
{
  "resource_id": "edge_gpu",
  "location": "edge",
  "models": ["small_model"],
  "latency": 0.2,
  "cost": 0.3,
  "security_level": "private",
  "available": true
}
然后让节点根据以下条件选择位置：
推理复杂度；
数据敏感度；
延迟要求；
资源可用性；
Token 或计算预算。
至少演示：
敏感文件提取 → 端侧/边缘
复杂建模推理 → 云端
资源离线 → 自动切换位置
这是任务书明确要求、当前又几乎空白的一项，补齐后的得分收益会比较高。
第四优先级：证明低熵通信真的降低了成本
现在 AgentPrune-lite、LLMLingua 和结构化消息已经接入，但评委看重的是“是否有效控制冗余信息”。
需要形成对比：
静态全连接 + 完整历史
vs
动态 DAG + 直接依赖 + AgentPrune-lite
记录：
总消息数；
总 Token；
原始上下文量；
实际传输量；
重复消息率；
压缩比；
最终任务质量；
总运行时间。
如果只展示代码中存在 LLMLingua 接口，技术创新分不会很高；必须用数据证明信息瓶颈和稀疏通信有效。
第五优先级：长程记忆保持机制
任务书真正要求的是：
关键任务信息不丢失；
中间决策不丢失；
全局目标不漂移；
不依赖单模型长窗口。
下一步应该强化：
全局目标保护区；
未解决问题记忆；
决策版本和冲突检测；
节点相关记忆检索；
过期记忆淘汰；
中断后记忆恢复。
Requirement Closure 可以作为其中的“目标和约束保护”辅助机制，但不是主角。
修正后的系统开发顺序
如果你暂时不做多场景，我建议按以下顺序：
1. 断点续跑和幂等恢复
2. 阶段性子图扩展，让任务可以持续展开
3. 运行期拓扑调整和局部重规划
4. 长程记忆唤醒、冲突和目标保护
5. 端—边—云 Resource Registry 与模拟调度
6. 低熵通信的真实指标与对比
7. 受控并行调度
8. 最后再补 Requirement Closure 和正式多场景验证
其中 Requirement Closure 不需要删除，保留现有 Requirement Contract 和阻断问题账本即可；暂时不必把它作为下一阶段最大工程。
最准确的结论是：
“统一任务完成判定”不是任务书直接规定的功能，而是防止错误成功的工程保障；从比赛评分看，当前更应该优先完成数千步长程能力、运行期动态拓扑、长程记忆和端边云资源调度。







按照任务书第六部分的评分标准（总分 100 分）来看，我对当前作品的客观估计是：

> 当前工程状态约 50～55 分；如果只看架构展示，可能达到 60 分左右；如果现在直接正式答辩，难以稳定超过 60 分。

这个估计依据的是当前代码、测试结果和真实运行证据，而不是未来规划。

## 一、当前分数估计

### 1. 作品完整性：约 24/40

| 评分项 | 满分 | 当前估计 | 原因 |
|---|---:|---:|---|
| 系统基础与闭环能力 | 15 | 10 | 已有 TaskSpec、动态 DAG、真实执行、错误修复、最终验收，但没有完整断点恢复和逐条需求闭环 |
| 组织架构与协作机制 | 15 | 11 | 已有 Capability Registry、动态路由、AgentPrune-lite、DyLAN-lite、EvidenceVerifier，但缺少并行调度、条件边、端边云资源调度 |
| 多场景任务演示 | 10 | 3 | 报销任务能运行但业务验收不够强；数学建模还不稳定；软件工程任务尚未完成 |

任务书明确要求至少两个高完成度跨领域任务，并且软件工程任务属于 MUST 要求：

- 文档/业务任务；
- 系统级软件工程任务；
- 数学建模属于 SHOULD。

目前最严重的扣分项就是“完整演示场景不足”，见 [PROJECT_REQUIREMENTS.md](/nfsdat1/home/rfchenslm/autogen_project/PROJECT_REQUIREMENTS.md:409)。

### 2. 应用创新性：约 7/25

| 评分项 | 满分 | 当前估计 | 原因 |
|---|---:|---:|---|
| 场景覆盖与业务契合度 | 10 | 4 | 框架通用性较好，但还没有两个完整业务场景证明泛化能力 |
| 社会价值与经济效益 | 10 | 2 | 目前主要展示技术过程，缺少成本、效率、人工替代和业务收益数据 |
| 用户体验与易用性 | 5 | 1 | 目前主要依赖运行目录、Trace 和 JSON，没有面向评委的进度、拓扑、风险和产物界面 |

这部分不是继续增加 Agent 能解决的，而是需要把系统运行结果转化为清晰的演示和业务价值。

### 3. 技术创新性：约 11/20

| 评分项 | 满分 | 当前估计 | 原因 |
|---|---:|---:|---|
| 架构与交互降噪 | 10 | 7 | 动态 DAG、稀疏路由、结构化通信、证据引用已经成型 |
| 核心算法与底层突破 | 10 | 4 | 记忆、压缩、DyLAN-lite 已接入，但还缺少长程压力数据、运行期拓扑重构和增量重规划 |

当前技术架构是有亮点的，但评委可能会追问：

- 压缩前后到底节省了多少 Token？
- 长程运行几百步后关键约束是否还在？
- 拓扑是否会随任务失败或资源变化重新生成？
- AgentPrune 和 DyLAN 是实际发挥作用，还是只记录了接口？

目前这些问题还缺乏量化证据。

### 4. 系统性能与效率：约 9/15

| 评分项 | 满分 | 当前估计 | 原因 |
|---|---:|---:|---|
| 执行成功率与鲁棒性 | 5 | 3 | 代码错误已能自动修复，证据不足时不会错误 success，但异常矩阵还未完整验证 |
| Token 与时间效率 | 5 | 2 | 有统计指标，但真实压缩比、并行加速和通信收益不稳定 |
| 兼容性与可扩展性 | 5 | 4 | Capability Registry、统一执行器和模型适配较好，但资源调度还没实现 |

任务书要求采集任务成功率、异常恢复率、误报成功率、检查点恢复率、状态保持率、压缩比等指标，见 [PROJECT_REQUIREMENTS.md](/nfsdat1/home/rfchenslm/autogen_project/PROJECT_REQUIREMENTS.md:496)。

因此当前总分大致为：

```text
作品完整性       24 / 40
应用创新性        7 / 25
技术创新性       11 / 20
系统性能效率      9 / 15
--------------------------------
合计              51 / 100
```

## 二、最影响得分的不是代码数量，而是三件事

### 第一，缺少两个完整且可验收的跨领域任务

当前报销任务已经能证明：

```text
动态规划
→ 多 Agent 执行
→ 代码错误自动修复
→ 真实执行
→ EvidenceVerifier 阻断错误结论
```

但这还不能证明业务结果已经正确完成。

下一步应该优先完成：

1. 文档业务任务；
2. 系统级软件工程任务。

软件工程任务必须体现：

```text
需求分析
→ 架构设计
→ 多文件编码
→ 测试执行
→ 测试失败
→ 自动修复
→ 中途需求变更
→ 局部重规划
→ 最终交付
```

这项直接关系到作品完整性中的 10 分场景演示，也是任务书中的 MUST 要求。

### 第二，缺少逐条需求验收

现在系统能判断：

- 文件是否存在；
- JSON 是否可解析；
- 代码是否执行成功；
- 是否有明显占位逻辑；
- EvidenceVerifier 是否找到证据问题。

但还不能统一回答：

> 题目要求的每一项是否都已经完成？

应该优先实现通用的 `Requirement Closure`：

```text
Requirement
→ 由哪些节点负责
→ 生成哪些产物
→ 用什么方法验证
→ passed / failed / unverified
```

最终成功条件应该是：

```python
execution_success
and artifact_success
and business_success
and delivery_success
and all_mandatory_requirements_verified
and not unresolved_blocking_issues
```

这会明显提高“基础闭环”和“业务可信度”评分。

### 第三，长程能力还没有数据证明

现在有 RuntimeMemory、Context Capsule 和上下文压缩接口，但还不能直接证明已经满足“超长程上下文连续性”。

至少需要做三组压力测试：

```text
100 节点
500 节点
1000 节点
```

每隔若干节点插入：

- 全局目标；
- 关键约束；
- 中间决策；
- 错误信息；
- 已验证产物；
- 未解决问题。

记录：

- 目标保持率；
- 约束保持率；
- 关键事实召回率；
- 错误记忆污染率；
- 压缩前后 Token；
- 任务恢复后状态一致率。

没有这些数据，评委很容易认为“长程记忆只是架构描述”。

## 三、下一步按得分收益排序

### P0：完成两个高质量演示任务

优先级最高。

建议顺序：

1. 完善文档业务任务的真实业务验收；
2. 完成软件工程长程任务；
3. 数学建模作为第三个增强场景。

每个任务都要保存：

- TaskSpec；
- Requirement Closure；
- TaskGraph；
- Agent Trace；
- Recovery Ledger；
- Artifact；
- Validation Report；
- 指标汇总。

### P1：实现 Requirement Closure

这是当前“系统是否真的完成任务”的核心补强。

没有它，系统只能说明“流程执行完了”，不能说明“题目要求完成了”。

### P1：实现断点续跑

任务书明确要求服务器中断后可以从检查点恢复。

需要支持：

```bash
python main.py --resume outputs/runs/<run_id>
```

恢复时：

- 已验证节点不重复执行；
- 未完成节点继续执行；
- 受影响子图重新执行；
- 产物哈希变化触发后继失效；
- RunState 和 TaskGraph 版本保持一致。

这项同时提升：

- 作品完整性；
- 鲁棒性；
- 长程任务可信度；
- 技术创新性。

### P1：做异常注入矩阵

至少准备以下演示：

| 异常 | 演示结果 |
|---|---|
| 代码语法错误 | 自动归因并修复 |
| Agent 被禁用 | 动态切换备选执行器 |
| 模型超时 | 重试或切换模型 |
| 结果产物错误 | 业务验证失败并重开责任节点 |
| 中途新增约束 | 受影响子图重规划 |
| 服务器中断 | checkpoint 恢复 |
| 证据不足 | 阻止错误 success |

当前已经真实验证了“代码错误自动修复”和“证据不足阻断”，但其他几项还需要补齐。

### P2：形成低熵通信和记忆的对比实验

不要只展示架构图，至少做三组对比：

```text
固定全连接通信
vs
直接依赖稀疏通信

完整历史上下文
vs
AgentPrune-lite + 内容引用

无记忆长上下文
vs
RuntimeMemory + Context Capsule
```

每组记录：

- Token；
- 字节；
- 重复消息率；
- 总耗时；
- 任务成功率；
- 关键状态保持率。

这才可以支撑技术创新性和系统效率评分。

### P3：准备一个简单的演示界面

当前文件和 JSON 适合调试，不适合答辩。

至少应展示：

- 当前阶段；
- DAG 节点状态；
- 当前路由到哪个 Agent；
- 失败原因；
- 修复次数；
- 通信拓扑；
- 记忆召回内容；
- 最终产物和验收结果。

不需要一开始建设复杂平台，一个静态 HTML 或 Streamlit 页面就可以显著改善作品呈现。

## 四、当前最合理的目标分数

如果按当前状态直接提交：

```text
约 50～55 分
```

如果完成以下四项：

1. 两个跨领域任务稳定通过；
2. Requirement Closure；
3. 断点续跑和异常注入；
4. 长程记忆与低熵通信对比实验；

预计可以提升到：

```text
70～80 分
```

如果再补齐：

- 端边云资源抽象；
- 可视化展示；
- 并行调度；
- 完整量化指标；
- 清晰的算法创新说明和复杂度分析；

才有机会冲击：

```text
80 分以上
```

因此，当前最优策略不是继续增加 Agent，而是：

> 先把两个跨领域任务做成“真实完成、可验收、可恢复、可复现”的演示，再用长程记忆、低熵通信和异常恢复数据证明技术创新。